// Паттерн Prototype позволяет создавать новые объекты путем клонирования существующих, а не через конструктор.

const carPrototype = {
  wheels: 4,
  start() {
    console.log("Engine started");
  },
  stop() {
    console.log("Engine stopped");
  },
};

// Создаем новый объект на основе прототипа
const myCar = Object.create(carPrototype);
myCar.model = "Toyota";

console.log(myCar.wheels); // 4 (унаследовано)
myCar.start(); // Engine started

// Преимущества:
// - Упрощение создания сложных объектов
// - Экономия ресурсов
// - Гибкость в runtime
// - Поддержка композиции вместо наследования

// Недостатки:
// - Сложность клонирования сложных объектов
// - Неочевидность поведения
// - Потеря контекста this при копировании методов
// - Сложность отладки